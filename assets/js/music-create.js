const musicForm=document.getElementById('createMusicForm');const musicMessage=document.getElementById('formMessage');
musicForm?.addEventListener('submit',async(event)=>{event.preventDefault();const button=musicForm.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Criando projeto...';try{const response=await fetch('api/music/create-track.php',{method:'POST',body:new FormData(musicForm),headers:{'X-CSRF-Token':window.MUSIC_CSRF}});const data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'Não foi possível criar.');location.href='minhas-musicas.php';}catch(error){musicMessage.hidden=false;musicMessage.className='alert error';musicMessage.textContent=error.message;button.disabled=false;button.textContent='Criar projeto';}});
